{
  "buildCommand": "npm install --legacy-peer-deps --prefix frontend && npm run build --prefix frontend",
  "outputDirectory": "frontend/build",
  "installCommand": "echo 'skip root install'",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
